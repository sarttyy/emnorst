
export const { prototype: ObjectPrototype } = Object;
export const { toString } = ObjectPrototype;
export const { prototype: ArrayPrototype } = Array;
export const { slice, push, pop } = ArrayPrototype;
